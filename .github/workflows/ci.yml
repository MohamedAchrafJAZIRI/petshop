- name: Check CSS files exist
        run: |
          if [ -f "css/home.css" ]; then
            echo "✅ home.css found"
          else
            echo "❌ home.css not found"
            exit 1
          fi